# Core data
pandas>=2.0.0
numpy>=1.26.0
scipy>=1.12.0
scikit-learn>=1.4.0

# Multi-agent orchestration
langgraph>=0.2.0
langchain-core>=0.2.0
langchain-openai>=0.1.0     # DeepSeek via OpenAI-compatible API

# Reporting
jinja2>=3.1.0
matplotlib>=3.7.0

# Webapp (FastAPI backend + SSE live timeline)
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9     # required by FastAPI for /upload endpoint
sse-starlette>=2.0.0

# Legacy Streamlit demo (archived in legacy/streamlit/, kept as fallback)
streamlit>=1.35.0
plotly>=5.18.0

# Notebook & env
python-dotenv>=1.0.0
jupyter>=1.0.0
nbconvert>=7.0.0
